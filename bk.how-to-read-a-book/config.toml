[garden]
  title   = 'garden-bed'
  root    = 'i.root'

[semtree]
  # candidate/experiment trees live in test/ — they deliberately re-tree the
  # same node names, so they must not participate in the hierarchy build
  exclude = ['test']

[format]
  attrs       = 'caml'
  mkdn_bullet = 'dash'
  indent_kind = 'space'
  indent_size = 2

[doc]
  [doc.kind]
    [doc.kind.default]
      path   = '/'
      color  = '#3e5c50'
    [doc.kind.zombie]
      emoji = '🧟'
      color = '#959DA5'
    [doc.kind.template]
      path   = '/template/'
      prefix = 't.' # '+'
      emoji  = '🧩'
      color  = '#F8F0E3'
